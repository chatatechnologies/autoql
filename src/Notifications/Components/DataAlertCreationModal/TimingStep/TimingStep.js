export function TimingStep({ queryResponse }) {
  const container = document.createElement('div');
  container.classList.add('autoql-vanilla-data-alert-modal-step');

  container.textContent = 'TimingStep';

  return container;
}